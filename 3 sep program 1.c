//write a program to calculate the area of circle
#define pi 3.14
main()
{
    float radius;
    printf("Enter the Radius of the Circle = ");
    scanf("%f",&radius);
    float area = pi * radius*radius;
    printf("The Area for the given radius is = %f2",area);
}
